 <!DOCTYPE html>
<html lang="ca">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registre d'Accessos</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-4">
        <h1>Registre d'Accessos</h1>
        <p>Total registres: <%= total %></p>
        
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>URL</th>
                    <th>Data i Hora</th>
                    <th>IP</th>
                    <th>Navegador</th>
                    <th>Mètode</th>
                </tr>
            </thead>
            <tbody>
                <% logs.forEach(log => { %>
                <tr>
                    <td><%= log.url %></td>
                    <td><%= log.timestamp.toLocaleString() %></td>
                    <td><%= log.ip %></td>
                    <td><%= log.userAgent %></td>
                    <td><%= log.method %></td>
                </tr>
                <% }); %>
            </tbody>
        </table>
        
        <!-- Paginació -->
        <nav aria-label="Paginació">
            <ul class="pagination">
                <% for(let i = 1; i <= pages; i++) { %>
                <li class="page-item <%= currentPage === i ? 'active' : '' %>">
                    <a class="page-link" href="?page=<%= i %>"><%= i %></a>
                </li>
                <% } %>
            </ul>
        </nav>
        
        <p class="mt-3">
            <a href="/admin/logs/stats" class="btn btn-primary">Veure Estadístiques</a>
            <a href="/" class="btn btn-secondary">Tornar a l'Inici</a>
        </p>
    </div>
</body>
</html>